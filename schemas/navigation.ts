import { defineField } from "sanity";
import {TbLayoutNavbar} from "react-icons/tb";

const navigation = {
    name: "navigation",
    title: "navigation",
    type: "array",
    icon: TbLayoutNavbar ,
    fields: [

    ],
};

export default navigation;
